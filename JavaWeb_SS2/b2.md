Ex02:
Phần 1:
        
| # | Vị trí | Loại vấn đề | Hậu quả |
|---|--------|-------------|---------|
| 1 | `<%! private int requestCounter = 0; %>` | Race Condition | Biến dùng chung giữa nhiều request → sai dữ liệu, không thread-safe |
| 2 | `<% for(...) { %>` | Logic trong View | Vi phạm nguyên tắc "View ngốc" → khó bảo trì |
| 3 | `<%= student.getName() %>` | Expression (cũ) | Không escape → dễ bị XSS |
| 4 | `<!-- comment -->` | HTML Comment sai chỗ | Vẫn hiển thị ra client → lộ code |
| 5 | `<%= ...; %>` | Sai cú pháp | Expression không cần ; → lỗi runtime |
| 6 | Java if/else trong JSP | Business logic trong View | Code rối, khó test |
| 7 | In dữ liệu user trực tiếp | XSS | Người dùng inject script |

Phần 2: 
    report.jsp
        <%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
        <%-- Trang báo cáo sinh viên --%>
        <html>
        <head>
            <title>Student Report</title>
        </head>
        <body>

        <h2>Danh sách sinh viên</h2>

        <table border="1">
            <tr>
                <th>Tên</th>
                <th>Điểm</th>
                <th>Xếp loại</th>
            </tr>

            <c:forEach var="student" items="${students}">
                <tr>
                    <td>
                        <c:out value="${student.name}" />
                    </td>
                    <td>
                        <c:out value="${student.score}" />
                    </td>
                    <td>
                        <c:choose>
                            <c:when test="${student.score >= 8}">
                                Giỏi
                            </c:when>
                            <c:when test="${student.score >= 5}">
                                Khá
                            </c:when>
                            <c:otherwise>
                                Yếu
                            </c:otherwise>
                        </c:choose>
                    </td>
                </tr>
            </c:forEach>

        </table>

        </body>
        </html>